import supertest from "supertest";
import app from "@/app.js";
import { describe } from "node:test";

describe("/register", () => {
  describe("given a username and password", () => {
    //should create a user and return said user
  });
});
